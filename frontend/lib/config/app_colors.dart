import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF1F3E0);
  static const Color surface = Color(0xFFD2DCB6);
  static const Color primary = Color(0xFFA1BC98);
  static const Color text = Color(0xFF778873);

  static const Color white = Colors.white;
  static const Color black = Colors.black;

  static final Color textSecondary = text.withValues(alpha: 0.8);
  static final Color textHint = text.withValues(alpha: 0.6);
}
